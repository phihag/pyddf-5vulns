create an example Django application
web:
	Login + logout
	security model
	SOP

	CSRF
	XSS
	DNS rebinding attack
	rel = noopener
	CSP
go through it all
names for every slide

reduce font size?

validate html
w3af / wapiti / ZAP
makefile to create pdf?
further topics:
	HTTP header injection
	Client-side security
	missing auth
	insecure password storage
